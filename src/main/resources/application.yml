server:
  port: 8888

spring:  cloud:
    config:
      server:
        git:
          uri: https://github.com/AntonioJRM1998/config-server.git
          default-label: main
          search-paths:          - '*service'
     